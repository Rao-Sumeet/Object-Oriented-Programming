#include<stdio.h>   //length of string ;
void main()
{  
  char str[5] ;
  int i; 
printf("enter string of name : \n");
  for(i=0;str[i]='\0';i++)
 { 
   scanf("%c", &str[i]);
 }

for(i=0;str[i]='\0';i++)

  {   
    printf( " %c" , str[i] );
  }
}
